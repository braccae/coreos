[Unit]
Description=Tailscale Connectivity Checker
After=network.target tailscaled.service
Requires=tailscaled.service

[Service]
Type=oneshot
ExecStart=bash /usr/libexec/coreos/tailscale-connectivity-check.sh
TimeoutStartSec=90s

[Install]
WantedBy=multi-user.target
name: Alma Container

on:
  push:
    branches: ["main"]
    paths:
      - "Containerfile"
      - "rootfs/common/**"
      - "build/scripts/**"
      - ".github/workflows/container.yml"
  workflow_dispatch:
  schedule:
    - cron: "0 5 1/4 * *"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: alma
  VARIENT: latest
  IMAGE_NAME_BASE: "alma"
  IMAGE_DESC: "alma bootc"
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"
  ARTIFACTHUB_LOGO_URL: "https://avatars.githubusercontent.com/u/23002397?v=4&size=64"

jobs:
  kernel-check:
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
    outputs:
      mismatch: ${{ steps.check.outputs.mismatch }}
    steps:
      - name: Login to GitHub Container Registry
        uses: redhat-actions/podman-login@v1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Check Kernel Mismatch
        id: check
        run: |
          # Pull base image and extract kernel version
          podman pull quay.io/almalinuxorg/almalinux-bootc:10
          BASE_KERNEL=$(podman run --rm quay.io/almalinuxorg/almalinux-bootc:10 sh -c 'basename /lib/modules/* | head -1')
          echo "Base image kernel: $BASE_KERNEL"
          
          # Pull kmods image and extract Alma amd64 kernel version
          KMODS_IMAGE="ghcr.io/${{ github.repository_owner }}/kmods:latest"
          podman pull $KMODS_IMAGE || true
          
          # Use podman create/cp since it is a scratch image
          CONTAINER_ID=$(podman create $KMODS_IMAGE true)
          if [ -n "$CONTAINER_ID" ]; then
            podman cp "$CONTAINER_ID:/zfs/alma/amd64/kernel-version.txt" ./kmod-version.txt 2>/dev/null || echo "none" > ./kmod-version.txt
            KMOD_KERNEL=$(cat ./kmod-version.txt)
            podman rm "$CONTAINER_ID"
          else
            KMOD_KERNEL="none"
          fi
          echo "Kmods image kernel: $KMOD_KERNEL"
          
          if [ "$BASE_KERNEL" != "$KMOD_KERNEL" ]; then
            echo "mismatch=true" >> $GITHUB_OUTPUT
            echo "::warning ::Kernel mismatch detected (Base: $BASE_KERNEL, Kmods: $KMOD_KERNEL). Triggering kmods build..."
            gh workflow run container-kmods.yml -R ${{ github.repository }} --ref ${{ github.ref_name }}
          else
            echo "mismatch=false" >> $GITHUB_OUTPUT
            echo "Kernel versions match ($BASE_KERNEL)."
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-metadata:
    runs-on: ubuntu-latest
    needs: kernel-check
    if: always() && needs.kernel-check.result == 'success' && needs.kernel-check.outputs.mismatch == 'false'
    outputs:
      tags: ${{ steps.meta.outputs.tags }}
      labels: ${{ steps.meta.outputs.labels }}
    steps:
      - name: Checkout
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98

      - name: Get current date
        id: date
        run: |
          echo "date=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_OUTPUT

      - name: Set Image Name and Tag
        id: set_image_name
        run: |
          BRANCH_NAME="${GITHUB_REF#refs/heads/}"
          if [[ "$BRANCH_NAME" == "main" ]]; then
            IMAGE_NAME="${{ env.IMAGE_NAME_BASE }}"
            TAG=${{ env.VARIENT }}
          else
            IMAGE_NAME="${{ env.IMAGE_NAME_BASE }}-${BRANCH_NAME}"
            TAG="$BRANCH_NAME"
          fi
          echo "IMAGE_NAME=$IMAGE_NAME" >> $GITHUB_OUTPUT
          echo "TAG=$TAG" >> $GITHUB_OUTPUT
          echo "IMAGE_NAME=$IMAGE_NAME" >> $GITHUB_ENV
          echo "DEFAULT_TAG=$TAG" >> $GITHUB_ENV

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ env.VARIENT }}
            type=sha,format=short
            type=ref,event=branch
            type=ref,event=pr
            type=schedule,pattern={{date 'YYYYMMDD'}}
          labels: |
            io.artifacthub.package.readme-url=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME_BASE }}/refs/heads/main/README.md
            org.opencontainers.image.created=${{ steps.date.outputs.date }}
            org.opencontainers.image.description=${{ env.IMAGE_DESC }}
            org.opencontainers.image.documentation=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME_BASE }}/refs/heads/main/README.md
            org.opencontainers.image.source=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME_BASE }}/blob/main/Containerfile
            org.opencontainers.image.title=${{ env.IMAGE_NAME }}
            org.opencontainers.image.url=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME_BASE }}
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.version=${{ env.DEFAULT_TAG }}
            io.artifacthub.package.deprecated=false
            io.artifacthub.package.keywords=bootc,alma
            io.artifacthub.package.license=Apache-2.0
            io.artifacthub.package.logo-url=${{ env.ARTIFACTHUB_LOGO_URL }}
            io.artifacthub.package.prerelease=false
            containers.bootc=1
          sep-tags: " "
          sep-annotations: " "

  build:
    runs-on: ${{ matrix.runner }}
    needs: build-metadata
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          - arch: amd64
            platform: linux/amd64
            runner: ubuntu-24.04
          - arch: arm64
            platform: linux/arm64
            runner: ubuntu-24.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Login to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: redhat-actions/podman-login@v1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Image
        uses: redhat-actions/buildah-build@v2
        id: build
        with:
          context: .
          containerfiles: ./Containerfile
          platforms: ${{ matrix.platform }}
          image: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
          tags: ${{ needs.build-metadata.outputs.tags }}
          labels: ${{ needs.build-metadata.outputs.labels }}
          extra-args: |
            --secret id=LOCALMOK,env=LOCALMOK
            --build-arg KMODS_IMAGE=${{ env.REGISTRY }}/${{ github.repository_owner }}/kmods:latest

      - name: Push To Registry
        if: github.event_name != 'pull_request'
        uses: redhat-actions/push-to-registry@v2
        id: push
        with:
          image: ${{ steps.build.outputs.image }}
          tags: ${{ steps.build.outputs.tags }}

      - name: Upload build info
        if: github.event_name != 'pull_request'
        run: |
          mkdir -p build-info
          echo "${{ steps.push.outputs.digest }}" > build-info/digest.txt
          echo "${{ matrix.platform }}" > build-info/platform.txt
        id: build-info

      - name: Upload artifact for manifest creation
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v7
        with:
          name: build-info-${{ matrix.arch }}
          path: build-info/

  create-manifest:
    runs-on: ubuntu-latest
    needs: [build-metadata, build]
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
      packages: write
    steps:
      - name: Login to GitHub Container Registry
        uses: redhat-actions/podman-login@v1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Download amd64 build artifacts
        uses: actions/download-artifact@v8
        with:
          name: build-info-amd64
          path: artifacts/build-info-amd64/

      - name: Download arm64 build artifacts
        uses: actions/download-artifact@v8
        with:
          name: build-info-arm64
          path: artifacts/build-info-arm64/

      - name: Read build info
        id: read-build-info
        run: |
          AMD64_DIGEST=$(cat artifacts/build-info-amd64/digest.txt)
          ARM64_DIGEST=$(cat artifacts/build-info-arm64/digest.txt)
          echo "amd64_digest=$AMD64_DIGEST" >> $GITHUB_OUTPUT
          echo "arm64_digest=$ARM64_DIGEST" >> $GITHUB_OUTPUT

      - name: Create and push multi-arch manifest
        id: push-manifest
        run: |
          REPOSITORY="${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}"
          for IMAGE_TAG in ${{ needs.build-metadata.outputs.tags }}; do
            echo "Creating and pushing manifest for $IMAGE_TAG"
            podman manifest create "$IMAGE_TAG"
            podman manifest add "$IMAGE_TAG" \
              --arch amd64 \
              --os linux \
              "$REPOSITORY@${{ steps.read-build-info.outputs.amd64_digest }}"
            podman manifest add "$IMAGE_TAG" \
              --arch arm64 \
              --os linux \
              "$REPOSITORY@${{ steps.read-build-info.outputs.arm64_digest }}"
            DIGEST=$(podman manifest push "$IMAGE_TAG")
            TAG="${IMAGE_TAG##*:}"
            if [[ "$TAG" == "${{ env.VARIENT }}" ]]; then
              echo "digest=${DIGEST}" >> "$GITHUB_OUTPUT"
            fi
          done

      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6

      - name: Sign multi-arch manifest
        run: |
          REPOSITORY="${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}"
          cosign sign -y --key env://COSIGN_PRIVATE_KEY "${REPOSITORY}:${{ env.VARIENT }}"
        env:
          COSIGN_EXPERIMENTAL: false
          COSIGN_PRIVATE_KEY: ${{ secrets.SIGNING_SECRET }}